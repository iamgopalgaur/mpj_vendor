import 'package:flutter/material.dart';

class JewelleryCollection extends StatelessWidget {
  const JewelleryCollection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      floatingActionButton: FloatingActionButton(
        onPressed: () {},
      ),
      body: Container(),
    );
  }
}
